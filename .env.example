# Optional research providers. The template works without any of these and
# falls back to generic Fabric MCP search (mcp.$search). Never commit real values.
# OmniRoute (general web search/fetch) is configured as a local MCP endpoint and
# needs no key here. Context7 and DeepWiki are optional lanes.
CONTEXT7_API_KEY=
DEEPWIKI_API_KEY=
