# Optional MCP providers. Copy this file to .env and fill the values to
# activate a provider. The template works without either provider and falls
# back to generic Fabric MCP search (mcp.$search). Never commit real values.
EXA_API_KEY=
DEEPWIKI_API_KEY=
