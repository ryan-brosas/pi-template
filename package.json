{
  "name": "pi-template",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Fabric-first pi.dev project template: triggerable skill packs, detailed research routing (OmniRoute, Context7, DeepWiki), honest extension status, and source provenance.",
  "license": "MIT",
  "scripts": {
    "validate:structure": "node scripts/validate-structure.mjs",
    "validate:config": "node scripts/validate-config.mjs",
    "validate:packs": "node scripts/validate-packs.mjs",
    "validate:research": "node scripts/validate-research.mjs",
    "validate:skills": "node scripts/validate-skills.mjs",
    "validate:prompts": "node scripts/validate-prompts.mjs",
    "validate:mcp": "node scripts/validate-mcp.mjs",
    "validate:sources": "node scripts/validate-sources.mjs",
    "scan:secrets": "node scripts/scan-secrets.mjs",
    "sync:sources": "node scripts/sync-sources.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test tests/*.test.mjs",
    "smoke:install": "node scripts/smoke-install.mjs",
    "check": "npm run typecheck && npm run validate:structure && npm run validate:config && npm run validate:packs && npm run validate:research && npm run validate:skills && npm run validate:prompts && npm run validate:mcp && npm run validate:sources && npm run scan:secrets && npm test && npm run smoke:install"
  },
  "dependencies": {
    "typebox": "1.3.7"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.84.1",
    "@types/node": "~24.12.4",
    "typescript": "~5.9.3"
  }
}