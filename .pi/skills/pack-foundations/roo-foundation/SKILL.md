---
name: roo-foundation
description: "Use when building IDE-embedded coding agents: mode-orchestrated personas with filtered tool surfaces, mistake-limit circuit breakers, pure auto-approval classifiers, and per-task shadow checkpoints."
disable-model-invocation: true
---
# Roo Foundation

## Use this for
IDE-embedded coding agents: mode-orchestrated personas with filtered tool surfaces, a mistake-limit circuit breaker, a pure auto-approval classifier, and per-task shadow checkpoints. Source and tests are the contract; the capsules carry decisive excerpts and actions.

## Load the matching source dump
- `references/task-loop.md` — Task structure, promise gates, recursive request stack, mistake circuit breaker, subtask delegation.
- `references/modes.md` — mode configs, system-prompt advertisement, alias-group tool filtering, composable prompt sections.
- `references/approvals.md` — pure auto-approval classifier, repetition detector, shadow checkpoints, say/ask UI protocol.

## Capsule map
- **Task loop** — `references/task-loop.md`: promise-gated late config, consecutive-mistake circuit breaker, per-repo checkpoint undo.
- **Modes & approvals** — `references/modes.md`, `references/approvals.md`: data-driven personas, cached tool filtering, pure auto-approval classifier.

## Extending the foundation
Add one references-file capsule per seam (loader line, grouped map, decisive source, invariant, probe, retrieval).

## Provenance
Indexed in Codebase Memory as `Roo-Code` (`/mnt/hdd/utopia/inspo/Roo-Code`); 36,333 nodes / 77,100 edges. Confirm every claim against source — the graph is an index, not truth.

## Boundaries
Adopt mode-orchestrated tool surfaces, mistake limiters, and pure classifiers; adapt the IDE provider and transport; omit site/webserate productization unless a target requires it.