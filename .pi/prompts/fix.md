---
description: Debug and fix a failing behavior (debugging-and-error-recovery). Reproduce, isolate, fix, verify with a regression test.
---

Invoke `debugging-and-error-recovery` (`.pi/skills/`). Read the full error,
reproduce, localize the layer, reduce to the smallest failing case, then fix
and guard with a regression test. Mutation only after the prewalk checklist is
accepted; record evidence for the prewalk schema.
