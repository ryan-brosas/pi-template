---
description: Create a specification (spec-driven-development + brainstorming). Outputs the spec that feeds the prewalk checklist.
---

Invoke `brainstorming` then `spec-driven-development` (`.pi/skills/`). Refine
the idea into a concrete spec: goal, non-goals, user stories, acceptance
criteria, open questions. The spec is the input for the prewalk checklist;
never mutate before prewalk accepts it.
