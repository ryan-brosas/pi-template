# Tech Stack

Auto-generated by `/init --deep --all` on 2026-08-09. Read this file on demand for project context. Regenerate it when the detected stack or repository constraints change.

## Project Classification

- **Project:** `ryan-brosas/pi-template`
- **Type:** Clonable Pi coding-agent project template
- **Primary runtime:** Pi (`pi-coding-agent`) with Ultra Fabric
- **Inspiration:** `inspo/opencode-template`, adapted to Pi-native prompts, skills, settings, and mutation safeguards
- **Audience:** Developers who want a stable starting point for Pi projects
- **Application runtime:** None. This repository is configuration and documentation, not a deployed application.

Evidence: `README.md:1-12`; authenticated `gh repo view` metadata on 2026-08-09.

## Languages and File Formats

| Area | Format | Purpose |
| --- | --- | --- |
| Agent rules and documentation | Markdown | Repository instructions, project context, and operator guidance |
| Prompt commands | Markdown | Pi slash-command prompt templates under `.pi/prompts/` |
| Skills | Markdown with YAML frontmatter | Reusable task-specific agent guidance under `.pi/skills/` |
| Pi settings | JSON | Thinking level, theme, and compaction configuration |
| Ultra Fabric settings | JSON | Prewalk and mutation-guard configuration |

There is no `src/`, `lib/`, or `app/` source tree and no indexed application code.

## Framework, Runtime, and Dependencies

- **Framework:** None
- **Build system:** None
- **Package manager:** None. No package manager or lockfile exists in the current tree
- **Project manifest:** None; no `package.json`, `pyproject.toml`, `Cargo.toml`, `go.mod`, or Makefile exists
- **Lockfile:** None in the current tree
- **Project dependencies:** None
- **Install step:** None
- **Development server:** None

`README.md:9-12` explicitly states that no package install is needed and that the repository has no package manifest, build, dependencies, or runtime harness.

### Host tools detected

These programs are available in the local environment. They are not project dependencies and do not establish a project package manager.

| Tool | Detected version |
| --- | --- |
| Node.js | v26.5.0 |
| npm | 12.0.1 |
| pnpm | 11.3.0 |
| Bun | 1.3.14 |
| Python | 3.14.6 |
| GNU Make | 4.4.1 |

Do not add commands based only on this host inventory. A project command requires a manifest, script, Make target, workflow step, or explicit user decision.

## Pi and Ultra Fabric Configuration

### Pi

`.pi/settings.json` currently configures:

- Default thinking level: `high`
- Theme: `dark`
- Compaction: enabled
- Compaction reserve: 16,384 tokens
- Recent context retained during compaction: 20,000 tokens

### Ultra Fabric

`.pi/fabric.json` configures the prewalk guard. Repository rules make prewalk the progression authority for mutations:

1. Research, discovery, and preview are read-only.
2. Non-trivial mutations require an accepted checklist with a Schema contract.
3. The executor owns declared writes and verification after handoff.
4. Unrelated working-tree changes must remain untouched.

## Repository Structure

| Path | Purpose |
| --- | --- |
| `AGENTS.md` | Always-on project rules for coding agents |
| `README.md` | Installation, command catalog, layout, and customization guidance |
| `.pi/fabric.json` | Ultra Fabric guard configuration |
| `.pi/settings.json` | Pi-native runtime preferences |
| `.pi/prompts/` | Nine project slash commands: `/init`, `/create`, `/plan`, `/fix`, `/ship`, `/verify`, `/audit`, `/gc`, `/research` |
| `.pi/skills/` | Sixty-two portable skills and the skill manifest |
| `.pi/templates/` | Eleven reusable artifact templates |
| `.pi/roadmap.md` | On-demand project direction and milestone context |
| `.pi/state.md` | Current position, decisions, risks, and next actions |
| `.pi/user.md` | On-demand user identity and workflow preferences |
| `.pi/artifacts/MEMORY.md` | Ignored local durable decisions and gotchas |

Evidence: `README.md:21-39` and the 2026-08-09 repository inventory.

## Commands and Verification

No build, test, lint, typecheck, format, or development commands exist in the current tree. Do not invent them.

For documentation and configuration changes, use these structural checks:

```bash
git diff --check
git status --short
```

Also read back every changed artifact and confirm required headings, evidence lines, and absence of unresolved placeholders. These are structural inspections, not application tests.

## Testing and CI/CD

- **Current test suite:** None in the working tree
- **Coverage target:** Not applicable
- **CI workflows:** None detected under `.github/workflows/`
- **Deployment:** None
- **Database or migrations:** None
- **User-facing UI:** None

Historical tests and validation scripts exist in Git history, but they are deleted in the current uncommitted cleanup. Initialization does not restore or replace them.

## Active Integrations

- **Pi:** Agent runtime and project-local prompts, skills, settings, and templates
- **Ultra Fabric:** Prewalk, Schema contract, model handoff, and mutation guard
- **codemap:** Semantic navigation when application source exists; the current configuration-only tree has no indexed source symbols
- **GitHub CLI:** Authenticated repository and user metadata lookup

No external application API, database, deployment provider, or credential-bearing integration is part of the template runtime.

## Key Constraints

1. Keep the repository clonable without a package-install step.
2. Preserve Pi-native operation; do not reintroduce OpenCode runtime wrappers as hidden dependencies.
3. Keep prewalk as the sole authority for non-trivial repository mutation.
4. Treat prompts, skills, templates, and settings as the product surface.
5. Preserve unrelated work in the large current working-tree cleanup.
6. Keep generated local state under `.pi/artifacts/`, `.pi/fabric/`, and `.pi/hindsight/` ignored.
7. Prefer stability and explicit verification over adding features or abstractions.
8. Record only detected values here; regenerate this file when repository facts change.

## Context Budget Guidelines

Target approximately half of the available context for one implementation plan. Split work when discovery and implementation span multiple subsystems, a task modifies more than five files, or verification needs a separate pass.

---

_Update this file when the detected stack, project commands, integrations, or constraints change._
